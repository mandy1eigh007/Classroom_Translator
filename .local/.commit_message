kids.html: swap waving-hand + action emoji for Dudu & Bubu sticker art

- Removed bg from 4 attached PNGs into public/avatars/:
  kids-welcome.png, kids-gotit.png, kids-help.png, kids-repeat.png
- Welcome/tap-to-start gate: replaced 👋 .gate-emoji with
  <img class="gate-emoji"> sized via min(70vw, 460px). Original
  bounce/drop-shadow animation kept.
- Lesson action row: replaced 👂 / 🙋 / 😊 inside .action-btn .emoji
  spans with <img> tags so existing layout, hover, and "Sent ✓" text
  swap on Help all still work unchanged.
- Added two small CSS blocks: img.gate-emoji sizing, and
  .action-btn .emoji img (96px / 76px mobile).

No JS changes — helpBtn.querySelector('span:last-child') still targets
the label span next to the icon span.
